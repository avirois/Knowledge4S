pipeline {
    agent { docker { image 'python:3.9.2-alpine3.12' } }
    stages {
        stage('test') {
            steps {
                sh '''
                ls -la
                python -m venv venv
                source venv/bin/activate
                pip install -r requirements.txt
                '''
                    sh 'echo okay'
                    sh 'false'
            }
        }
    }
}
