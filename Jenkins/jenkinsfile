pipeline {
    agent { docker { image 'python:3.9.2-alpine3.12' } }
    stages {
        stage('getInfo') {
            sh 'ls -la'
            sh 'uname -a'
            sh 'pwd'
            sh 'false'
        }
    }
}
